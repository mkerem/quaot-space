{
  "name": "quote-constellation",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-embeddings": "node scripts/generate-embeddings.js",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.6.0",
    "@xenova/transformers": "^2.17.2"
  }
}
